/**
 * Minimal placeholder test so Jest has at least one test to run and doesn't fail the CI.
 */

describe("datastore - placeholder", () => {
  test("no-op placeholder", () => {
    expect(true).toBe(true);
  });
});
